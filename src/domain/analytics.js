const _analytics = {};

_analytics.getByFile = function getByFile({ contractAddress, fileId }) {}
_analytics.getByContract = function getByContract({ contractAddress }) {}

module.exports = _analytics;
